# 本手册交付核对

状态：**进行中，未完整交付**。已写入本地资料和独立代码样例，并通过 Browser Use 在已登录的 lili 账号下创建独立验收工作区和频道；未创建其他账号或成员，未变更密码、Computer 授权或服务运行方式。

| 用户要求 | 当前结果 | 尚需完成 |
| --- | --- | --- |
| 按不写代码的 PM／测试讲解 | 20 个用户故事；先解释入口与用户可见变化 | 用真实界面试走，进一步删除不必要的术语 |
| 前端入口 | 已对照当前组件、中文文案和真实 E2E 写入路径 | 登录后核对当前界面布局 |
| 截图并圈出操作位置 | 已定义 A01～A14 实拍清单 | A01、A02 已实拍并在 HTML 叠加编号红圈；另补 Computer 离线证据；A03～A14 与 A02 的 Agent／代码分支待补；未用示意图冒充 |
| 每步怎么操作、验证什么 | 正向、退回、上限、等待、权限与回归均有步骤及判断 | **新手册现场逐项试走待执行** |
| 不需要自己编造数据 | 固定名字、输入、提示词、审核文案、代码仓库、三个 Skill 包已给出 | 验收工作区和第一个频道已准备并查库确认；专用成员、初始 Skill、跨 owner 验收身份待准备 |
| 可省心执行的整套手册 | HTML 导航、13 个样例代码块可复制、20 个状态选择及导出、Markdown 源文 | 浏览器渲染／复制／导出交互尚未实际验证 |
| 真实测试结果 | 复用实现报告的 28 个真实 E2E 场景作为既有工程证据；新离线材料校验已完成 | 不能把既有 E2E 或离线自检当成本手册现场通过 |

离线校验详情：[离线材料校验.json](离线材料校验.json)。已核对全部本地链接、20 个用例锚点及记录控件、三套 Skill 内容，真实运行 Python 检查错误代码失败、正确代码通过，原代码样例仓库仍干净。

已通过 Browser Use 找到 Tabbit 的现有登录会话，当前用户为 lili，无需再登录该账号。在线 Computer「Studio Mac」属于 Fred；lili 当前能选的 Computer 离线。已请求用户切换到 Fred，或由用户处理 Computer 授权；不读取密码、浏览器令牌或 daemon 凭据替代登录，不直接改库授予权限。

内置浏览器拒绝打开本地 HTML 的 file URL；没有切换浏览器、代理地址或低层命令绕过。静态链接和文档结构检查可继续，但不能据此声称完成视觉检查。

下一步在已登录账号拥有可用的在线 Computer 后，继续准备已获授权的独立“本轮功能验收”样例，收集真实产品界面圈注并逐例走查。跨 owner 场景先确定第二个验收身份及其已授权 Computer；不把原 Computer 静默共享给新账号。然后逐条更新本核对表；所有要求实际满足后才宣布完成。

## 2026-09-10 续接结果

- 已在同一 Tabbit 页面重新确认 lili 登录、验收空间和空成员列表。
- 创建任务表单已填写固定样例，观察到填写验收要求后才出现验收方式／审核者；取消表单，未创建任务。已保存 A02 并加入六个圈号。
- 已保存当前账号 Computer 为 Offline 的实拍并圈注。真实 Agent 试走仍受此前置条件阻塞，用户已收到上线／切换账号的请求。
- API 与 daemon 健康正常，进程工作目录仍为 a8d8/solo；只读事务核对测试频道仍为 0 个任务、0 个 Agent。本轮未重启服务、未修改账号权限。
- 实际重新运行 5 次样例 self-check，通过；没有把离线算法检查算成 Runtime E2E。
- 逐例状态见 [现场试走记录.json](现场试走记录.json)：PM-01 阻塞，PM-02～20 未执行。

## 已完成的 Browser Use 操作

- 在实际界面点击 New workspace 并创建“本轮功能验收”。
- 通过 Create Channel → Blank Channel 创建 pm-交付验收。
- 保存 A01-workspace.png 原始截图；HTML 上叠加四个编号红圈，不改变截图内容。
- 打开实际 Create Agent 表单，确认 Computer 为 Offline，尚未提交新成员。
- 只读 PostgreSQL 确认 workspace/channel 已保存，频道任务数和成员数均为 0。
- 新建入口曾被自动审批因索引疑义拦截，重新读取完整 AX 并证实按钮名后重试成功；没有点击无关工作区。

原库服务健康检查：API readyz=ok，daemon health=ok、control_connected=true，3000/8080/8081 仍由原 make 管理进程提供。
