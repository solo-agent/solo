# 版本样例：基线 40 → 候选 42 → 接收方复核

固定输入 `[20,22]`。基线脚本真实执行 `sum(values)-2`，候选真实执行 `sum(values)`，接收方实际解析候选交付 JSON 后验证求和。

## 准备状态

三个 Skill 文件包已在本资料目录准备；在线 PM对照员与 PM接收员尚未创建。当前“新建成员”表单没有初始 Skill 上传入口，因此不能让 PM 自行点击完成这项前置准备。登录并确认准备验收数据后，由维护者通过已有成员 API 创建这两个**独立的测试成员**，附带下面的基线与接收方文件包。只使用该 owner 已授权的 Computer，不修改原业务成员。

候选上传入口已经存在：成员详情 → 能力版本与评测 → 加入 Skill 文件夹。选择下方候选文件夹本身，不要选择包含三套版本的总目录。

| 成员／用途 | 目录 | 预期 |
| --- | --- | --- |
| PM对照员初始 Skill | `skills/baseline/pm-arithmetic` | 40 |
| PM对照员候选 Skill | `skills/candidate/pm-arithmetic` | 42 |
| PM接收员初始 Skill | `skills/receiver/pm-arithmetic` | 验证确切候选输入与结果为 42 |

目录根位置：`/Users/langgengxin/.codex/worktrees/a8d8/solo/docs/acceptance/agent-team-compounding/fixtures/`。

## PM对照员与候选工作说明（复制全文）

```text
你是 PM对照员。入场时仅用 solo message send 发送 PM_SELECTION_READY 并结束。
收到分配给你的评测任务时，真实执行 python3 .agents/skills/pm-arithmetic/check.py 当前频道UUID 当前任务编号 一次。参数从本次任务上下文读取，不让 PM 编造。
脚本从当前任务读取固定输入；独立评测专用题固定为 [20,22]。脚本自己提交证据、发送结果；成功后结束，不再重复提交。
不要修改安装的脚本或 mode.json，不因基线算出 40 而替它改答案；本次就是要比较两个版本的真实输出。若 CLI 或程序报错，报告实际错误并停止，不寻找凭据、不读取其他成员目录、不启停服务。
```

## PM接收员工作说明（复制全文）

```text
你是 PM接收员。入场时仅用 solo message send 发送 PM_RECEIVER_READY 并结束。
收到接收检查任务时，从本次提示中找到指定候选 submission 的 E1 content，逐字取出完整 JSON，作为第三个参数执行：python3 .agents/skills/pm-arithmetic/check.py 当前频道UUID 当前任务编号 候选E1完整JSON。
用安全的参数传递保留 JSON 原文，不通过 shell 拼接可能被执行的内容。脚本实际解析候选证据并验证，提交时保留 received_candidate 原文。
禁止自行根据样例编造一个 E1 替代收到的候选。缺少候选证据就报告并停止。脚本已提交与发送，成功后结束，不再重复提交。禁止读取其他成员目录、环境变量、凭据或无关文件。
```

## 发布后的复验

当前普通创建任务弹窗没有描述字段，而此 Skill 从任务描述解析固定 JSON。可向已发布成员发送以下完整消息，让它通过现有任务 CLI 建立正确样例：

```text
请创建并认领一个专用测试任务，标题 PM发布后复验，description 精确为 [20,22]。通过真实当前 Solo CLI 创建与读取，不伪造任务。然后只执行已安装的 pm-arithmetic/check.py，保留真实输出并交给我验收，不自行审核。使用 solo task create 的 --description 参数保留这份精确 JSON，不自行改脚本。
```

回滚复验使用标题 `PM恢复后复验`，其余相同。恢复旧基线得到 40 是本例故意保留的行为；验收结束后恢复候选 42。

## 离线检查与真实执行的区别

`selection_check.py self-check` 只验证固定算法和接收方拒绝错误 JSON，不模拟 Solo 服务，也不代表产品 E2E 已跑过。真实 UI、API、数据库与 Runtime 的现场试走仍要在在线样例准备后完成。
